using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattleBrain : MonoBehaviour
{
    // a list of enemies that are alive
    public List<Enemy> enemiesOnScreen;

    // players
    public PlayerFighter playerOne;
    public PlayerFighter playerTwo;

    // if we've won or lost
    public bool hasWon;
    public bool hasLost;

    // Start is called before the first frame update
    void Start()
    {
        // TODO : ENEMIES PLACE (VISUAL) ON SCREEN DEPENDING ON NUMBER OF ENEMIES


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
